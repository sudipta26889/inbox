interface:
  display_name: "Inbox API CLI"
  short_description: "Manage Inbox rules and stats"
  default_prompt: "Use $inbox-zero-api to inspect or update Inbox rules and analytics through the API CLI."

policy:
  allow_implicit_invocation: true
